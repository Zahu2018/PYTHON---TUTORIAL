import module
dir()
dir(module)
dir(module.method)
help(dict)
help(math.method)
help()
any(); all()
-------------------------------------------------
pip install howdoi  # stackoverflow in consola
din consola NU PYTHON CONSOLA:
howdoi cauta ceva anume
-------------------------------------------------
**kwargs
dictionary = {"a": 1, "b": 2}
def someFunction(a, b):
    print(a + b)
    return
# these do the same thing: replace someFunction
someFunction(**dictionary)
someFunction(a=1, b=2)
-------------------------------------------------
numbers = [1,2,3,4,5,6,7]  # comprehension
evens = [x for x in numbers if x % 2 is 0]
odds = [y for y in numbers if y not in evens]

cities = ['London', 'Dublin', 'Oslo']
def visit(city):
    print("Welcome to "+city)
for city in cities:
    visit(city)
-------------------------------------------------
x = [1, 2, 3]  # map with lambda
y = map(lambda x : x + 1 , x)
# prints out [2,3,4]print(list(y))
-------------------------------------------------
pip install newspaper3k  # article
-------------------------------------------------
keys = ['a', 'b', 'c']
vals = [1, 2, 3]
zipped = dict(zip(keys, vals))
-------------------------------------------------

-------------------------------------------------

